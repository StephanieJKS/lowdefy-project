
lowdefy: 3.23.3
name: Lowdefy starter

types:
  AmChartsXY:
    url: https://blocks-cdn.lowdefy.com/v3.11.3/blocks-amcharts/meta/AmChartsXY.json
  AmChartsPie:
    url: https://blocks-cdn.lowdefy.com/v3.11.3/blocks-amcharts/meta/AmChartsPie.json
  AgGridAlpine:
    url: https://blocks-cdn.lowdefy.com/v3.10.1/blocks-aggrid/meta/AgGridAlpine.json

connections:
  - id: my_collection
    type: MongoDBCollection
    properties:
      databaseUri:
        _secret: MONGODB_URI
      collection: reviews
      write: true

  - id: movies_mongodb # The connectionId that will be used when defining requests and mutations on our pages
    type: MongoDBCollection
    properties:
      databaseName: sample_mflix # The database name
      collection: movies # The collection name
      databaseUri:
        _secret: MONGODB_URI # The database connection uri that is stored as a secret and accessed using the _secret operat

menus:
  - id: default
    links:
      - id: welcome
        type: MenuLink
        properties:
          icon: HomeOutlined
          title: Home
        pageId: welcome

      - id: review
        type: MenuLink
        properties:
          icon: EditOutlined
          title: Review Us
        pageId: review

      - id: results
        type: MenuLink
        properties:
          icon: BarChartOutlined
          title: Ratings
        pageId: results

      - id: written_reviews # Define the menu link that directs to the report page
        type: MenuLink
        pageId: written_reviews # Id of the report page
        properties:
          title: Written Reviews # Title to show on the menu
          icon: FileTextFilled

pages:
  - _ref: review.yaml
  - _ref: results.yaml
  - _ref: written_reviews.yaml
  - _ref: home.yaml

